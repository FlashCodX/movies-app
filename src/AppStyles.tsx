import styled from "styled-components";

/**Works as a the main app container.
 *  Fully responsive */
export const Application = styled.main`
  position: relative;
  display: grid;
  width: 100%;
  margin: auto;
  justify-content: center;
`;
